"""Filesystem-source pattern matching (``ci.load(match={...})``) — stub for SDK-13.

Per spec §4.7: ``path`` glob, ``filename`` regex, ``extension`` shorthand,
``columns`` pushdown for formats that support it.
"""
